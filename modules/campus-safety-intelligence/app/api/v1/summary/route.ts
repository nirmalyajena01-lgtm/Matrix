import{NextResponse}from'next/server';import{prisma}from'@/lib/prisma';export async function GET(){const e=await prisma.safetyEvent.findMany();const resolved=e.filter(x=>x.status==='RESOLVED').length;const avg=e.length?Math.round(e.reduce((a,x)=>a+x.responseMinutes,0)/e.length*10)/10:0;const critical=e.filter(x=>x.severity==='CRITICAL').length;const hotspots=Object.entries(e.reduce((a,x)=>{a[x.building]=(a[x.building]??0)+1;return a},{} as Record<string,number>)).sort((a,b)=>b[1]-a[1]).slice(0,5);return NextResponse.json({total:e.length,resolvedPercent:e.length?Math.round(resolved/e.length*100):0,avgResponseMinutes:avg,critical,hotspots})}